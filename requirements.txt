# Core
pymupdf>=1.25.0
requests>=2.32.0
Pillow>=11.0.0
python-dotenv>=1.0.0

# Backend Web
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# Motor ML (visión / OCR)
opencv-python-headless>=4.8.0

# NOTA: Las dependencias pesadas de manga-image-translator (PyTorch, numpy 1.26,
# OpenVINO, pydensecrf...) NO se listan aquí a propósito. El Dockerfile clona el
# motor a un ref fijo (ARG MIT_REF) e instala su requirements.txt desde el clone,
# lo que hace el build reproducible y evita instalarlas dos veces.
# Para una instalación nativa (Linux + Python 3.11), clona el motor manualmente:
#   git clone https://github.com/zyddnys/manga-image-translator.git
#   pip install -r manga-image-translator/requirements.txt